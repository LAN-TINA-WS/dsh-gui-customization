//#region src/index.ts
/**
* Host loader entry for the browser-only gui-customization plugin.
* Provides no host-side behavior（持久化走浏览器 localStorage/IndexedDB）。
*/
function apply() {}
//#endregion
export { apply };
